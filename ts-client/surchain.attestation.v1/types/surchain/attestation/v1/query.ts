// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: surchain/attestation/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";

export const protobufPackage = "surchain.attestation.v1";

/** QueryParamsRequest is request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params holds all the parameters of this module. */
  params: Params | undefined;
}

/** QueryIsNullifierUsedRequest is the request for IsNullifierUsed. */
export interface QueryIsNullifierUsedRequest {
  nullifier: Uint8Array;
}

/** QueryIsNullifierUsedResponse is the response for IsNullifierUsed. */
export interface QueryIsNullifierUsedResponse {
  used: boolean;
}

/** QueryGetAttestationRequest is the request for GetAttestation. */
export interface QueryGetAttestationRequest {
  username: string;
  nullifier: Uint8Array;
}

/** QueryGetAttestationResponse is the response for GetAttestation. */
export interface QueryGetAttestationResponse {
  username: string;
  contentHash: Uint8Array;
  nullifier: Uint8Array;
  timestamp: number;
}

/**
 * QueryVerifyContentRequest is the request for VerifyContent. content_hash_hex
 * is the hex-encoded SHA-256 of the content being checked.
 */
export interface QueryVerifyContentRequest {
  contentHashHex: string;
}

/** AttestationMatch is one attestation bound to the queried content. */
export interface AttestationMatch {
  username: string;
  nullifier: Uint8Array;
  commitmentRoot: Uint8Array;
  timestamp: number;
  /** Provenance class and optional citation (see AttestationRecord). */
  origin: string;
  citation: string;
}

/**
 * QueryVerifyContentResponse is the response for VerifyContent. found is true
 * when at least one attestation exists for the content; attestations lists them
 * all (the same text can be attested by multiple users/sessions).
 */
export interface QueryVerifyContentResponse {
  found: boolean;
  contentHash: Uint8Array;
  attestations: AttestationMatch[];
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryIsNullifierUsedRequest(): QueryIsNullifierUsedRequest {
  return { nullifier: new Uint8Array(0) };
}

export const QueryIsNullifierUsedRequest: MessageFns<QueryIsNullifierUsedRequest> = {
  encode(message: QueryIsNullifierUsedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nullifier.length !== 0) {
      writer.uint32(10).bytes(message.nullifier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryIsNullifierUsedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryIsNullifierUsedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nullifier = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryIsNullifierUsedRequest {
    return { nullifier: isSet(object.nullifier) ? bytesFromBase64(object.nullifier) : new Uint8Array(0) };
  },

  toJSON(message: QueryIsNullifierUsedRequest): unknown {
    const obj: any = {};
    if (message.nullifier.length !== 0) {
      obj.nullifier = base64FromBytes(message.nullifier);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryIsNullifierUsedRequest>, I>>(base?: I): QueryIsNullifierUsedRequest {
    return QueryIsNullifierUsedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryIsNullifierUsedRequest>, I>>(object: I): QueryIsNullifierUsedRequest {
    const message = createBaseQueryIsNullifierUsedRequest();
    message.nullifier = object.nullifier ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQueryIsNullifierUsedResponse(): QueryIsNullifierUsedResponse {
  return { used: false };
}

export const QueryIsNullifierUsedResponse: MessageFns<QueryIsNullifierUsedResponse> = {
  encode(message: QueryIsNullifierUsedResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.used !== false) {
      writer.uint32(8).bool(message.used);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryIsNullifierUsedResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryIsNullifierUsedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.used = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryIsNullifierUsedResponse {
    return { used: isSet(object.used) ? globalThis.Boolean(object.used) : false };
  },

  toJSON(message: QueryIsNullifierUsedResponse): unknown {
    const obj: any = {};
    if (message.used !== false) {
      obj.used = message.used;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryIsNullifierUsedResponse>, I>>(base?: I): QueryIsNullifierUsedResponse {
    return QueryIsNullifierUsedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryIsNullifierUsedResponse>, I>>(object: I): QueryIsNullifierUsedResponse {
    const message = createBaseQueryIsNullifierUsedResponse();
    message.used = object.used ?? false;
    return message;
  },
};

function createBaseQueryGetAttestationRequest(): QueryGetAttestationRequest {
  return { username: "", nullifier: new Uint8Array(0) };
}

export const QueryGetAttestationRequest: MessageFns<QueryGetAttestationRequest> = {
  encode(message: QueryGetAttestationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.nullifier.length !== 0) {
      writer.uint32(18).bytes(message.nullifier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetAttestationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetAttestationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nullifier = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetAttestationRequest {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      nullifier: isSet(object.nullifier) ? bytesFromBase64(object.nullifier) : new Uint8Array(0),
    };
  },

  toJSON(message: QueryGetAttestationRequest): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.nullifier.length !== 0) {
      obj.nullifier = base64FromBytes(message.nullifier);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetAttestationRequest>, I>>(base?: I): QueryGetAttestationRequest {
    return QueryGetAttestationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetAttestationRequest>, I>>(object: I): QueryGetAttestationRequest {
    const message = createBaseQueryGetAttestationRequest();
    message.username = object.username ?? "";
    message.nullifier = object.nullifier ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQueryGetAttestationResponse(): QueryGetAttestationResponse {
  return { username: "", contentHash: new Uint8Array(0), nullifier: new Uint8Array(0), timestamp: 0 };
}

export const QueryGetAttestationResponse: MessageFns<QueryGetAttestationResponse> = {
  encode(message: QueryGetAttestationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.contentHash.length !== 0) {
      writer.uint32(18).bytes(message.contentHash);
    }
    if (message.nullifier.length !== 0) {
      writer.uint32(26).bytes(message.nullifier);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryGetAttestationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetAttestationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nullifier = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetAttestationResponse {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      contentHash: isSet(object.contentHash)
        ? bytesFromBase64(object.contentHash)
        : isSet(object.content_hash)
        ? bytesFromBase64(object.content_hash)
        : new Uint8Array(0),
      nullifier: isSet(object.nullifier) ? bytesFromBase64(object.nullifier) : new Uint8Array(0),
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: QueryGetAttestationResponse): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.contentHash.length !== 0) {
      obj.contentHash = base64FromBytes(message.contentHash);
    }
    if (message.nullifier.length !== 0) {
      obj.nullifier = base64FromBytes(message.nullifier);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetAttestationResponse>, I>>(base?: I): QueryGetAttestationResponse {
    return QueryGetAttestationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetAttestationResponse>, I>>(object: I): QueryGetAttestationResponse {
    const message = createBaseQueryGetAttestationResponse();
    message.username = object.username ?? "";
    message.contentHash = object.contentHash ?? new Uint8Array(0);
    message.nullifier = object.nullifier ?? new Uint8Array(0);
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseQueryVerifyContentRequest(): QueryVerifyContentRequest {
  return { contentHashHex: "" };
}

export const QueryVerifyContentRequest: MessageFns<QueryVerifyContentRequest> = {
  encode(message: QueryVerifyContentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contentHashHex !== "") {
      writer.uint32(10).string(message.contentHashHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVerifyContentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVerifyContentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.contentHashHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVerifyContentRequest {
    return {
      contentHashHex: isSet(object.contentHashHex)
        ? globalThis.String(object.contentHashHex)
        : isSet(object.content_hash_hex)
        ? globalThis.String(object.content_hash_hex)
        : "",
    };
  },

  toJSON(message: QueryVerifyContentRequest): unknown {
    const obj: any = {};
    if (message.contentHashHex !== "") {
      obj.contentHashHex = message.contentHashHex;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVerifyContentRequest>, I>>(base?: I): QueryVerifyContentRequest {
    return QueryVerifyContentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVerifyContentRequest>, I>>(object: I): QueryVerifyContentRequest {
    const message = createBaseQueryVerifyContentRequest();
    message.contentHashHex = object.contentHashHex ?? "";
    return message;
  },
};

function createBaseAttestationMatch(): AttestationMatch {
  return {
    username: "",
    nullifier: new Uint8Array(0),
    commitmentRoot: new Uint8Array(0),
    timestamp: 0,
    origin: "",
    citation: "",
  };
}

export const AttestationMatch: MessageFns<AttestationMatch> = {
  encode(message: AttestationMatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.nullifier.length !== 0) {
      writer.uint32(18).bytes(message.nullifier);
    }
    if (message.commitmentRoot.length !== 0) {
      writer.uint32(26).bytes(message.commitmentRoot);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    if (message.origin !== "") {
      writer.uint32(42).string(message.origin);
    }
    if (message.citation !== "") {
      writer.uint32(50).string(message.citation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttestationMatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestationMatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nullifier = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commitmentRoot = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.origin = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.citation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttestationMatch {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      nullifier: isSet(object.nullifier) ? bytesFromBase64(object.nullifier) : new Uint8Array(0),
      commitmentRoot: isSet(object.commitmentRoot)
        ? bytesFromBase64(object.commitmentRoot)
        : isSet(object.commitment_root)
        ? bytesFromBase64(object.commitment_root)
        : new Uint8Array(0),
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      origin: isSet(object.origin) ? globalThis.String(object.origin) : "",
      citation: isSet(object.citation) ? globalThis.String(object.citation) : "",
    };
  },

  toJSON(message: AttestationMatch): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.nullifier.length !== 0) {
      obj.nullifier = base64FromBytes(message.nullifier);
    }
    if (message.commitmentRoot.length !== 0) {
      obj.commitmentRoot = base64FromBytes(message.commitmentRoot);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.origin !== "") {
      obj.origin = message.origin;
    }
    if (message.citation !== "") {
      obj.citation = message.citation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AttestationMatch>, I>>(base?: I): AttestationMatch {
    return AttestationMatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AttestationMatch>, I>>(object: I): AttestationMatch {
    const message = createBaseAttestationMatch();
    message.username = object.username ?? "";
    message.nullifier = object.nullifier ?? new Uint8Array(0);
    message.commitmentRoot = object.commitmentRoot ?? new Uint8Array(0);
    message.timestamp = object.timestamp ?? 0;
    message.origin = object.origin ?? "";
    message.citation = object.citation ?? "";
    return message;
  },
};

function createBaseQueryVerifyContentResponse(): QueryVerifyContentResponse {
  return { found: false, contentHash: new Uint8Array(0), attestations: [] };
}

export const QueryVerifyContentResponse: MessageFns<QueryVerifyContentResponse> = {
  encode(message: QueryVerifyContentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    if (message.contentHash.length !== 0) {
      writer.uint32(18).bytes(message.contentHash);
    }
    for (const v of message.attestations) {
      AttestationMatch.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVerifyContentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVerifyContentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.attestations.push(AttestationMatch.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVerifyContentResponse {
    return {
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
      contentHash: isSet(object.contentHash)
        ? bytesFromBase64(object.contentHash)
        : isSet(object.content_hash)
        ? bytesFromBase64(object.content_hash)
        : new Uint8Array(0),
      attestations: globalThis.Array.isArray(object?.attestations)
        ? object.attestations.map((e: any) => AttestationMatch.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryVerifyContentResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    if (message.contentHash.length !== 0) {
      obj.contentHash = base64FromBytes(message.contentHash);
    }
    if (message.attestations?.length) {
      obj.attestations = message.attestations.map((e) => AttestationMatch.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVerifyContentResponse>, I>>(base?: I): QueryVerifyContentResponse {
    return QueryVerifyContentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVerifyContentResponse>, I>>(object: I): QueryVerifyContentResponse {
    const message = createBaseQueryVerifyContentResponse();
    message.found = object.found ?? false;
    message.contentHash = object.contentHash ?? new Uint8Array(0);
    message.attestations = object.attestations?.map((e) => AttestationMatch.fromPartial(e)) || [];
    return message;
  },
};

/** Query defines the gRPC querier service. */
export interface Query {
  /** Parameters queries the parameters of the module. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  /** IsNullifierUsed checks whether a nullifier has been used before. */
  IsNullifierUsed(request: QueryIsNullifierUsedRequest): Promise<QueryIsNullifierUsedResponse>;
  /** GetAttestation retrieves an attestation by username and nullifier. */
  GetAttestation(request: QueryGetAttestationRequest): Promise<QueryGetAttestationResponse>;
  /**
   * VerifyContent checks the origin of a piece of content by its hash. Given
   * SHA-256(content) (hex), it returns every accepted human-typing attestation
   * bound to that content. This is the public provenance-verification endpoint:
   * a match proves the text was typed by a human on a registered Sur device.
   */
  VerifyContent(request: QueryVerifyContentRequest): Promise<QueryVerifyContentResponse>;
}

export const QueryServiceName = "surchain.attestation.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.IsNullifierUsed = this.IsNullifierUsed.bind(this);
    this.GetAttestation = this.GetAttestation.bind(this);
    this.VerifyContent = this.VerifyContent.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(new BinaryReader(data)));
  }

  IsNullifierUsed(request: QueryIsNullifierUsedRequest): Promise<QueryIsNullifierUsedResponse> {
    const data = QueryIsNullifierUsedRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "IsNullifierUsed", data);
    return promise.then((data) => QueryIsNullifierUsedResponse.decode(new BinaryReader(data)));
  }

  GetAttestation(request: QueryGetAttestationRequest): Promise<QueryGetAttestationResponse> {
    const data = QueryGetAttestationRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetAttestation", data);
    return promise.then((data) => QueryGetAttestationResponse.decode(new BinaryReader(data)));
  }

  VerifyContent(request: QueryVerifyContentRequest): Promise<QueryVerifyContentResponse> {
    const data = QueryVerifyContentRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "VerifyContent", data);
    return promise.then((data) => QueryVerifyContentResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
