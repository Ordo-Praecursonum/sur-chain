// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: surchain/identity/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";

export const protobufPackage = "surchain.identity.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /**
   * params defines the module parameters to update.
   *
   * NOTE: All parameters must be supplied.
   */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/** MsgRegisterUsername registers a new username with an initial device commitment. */
export interface MsgRegisterUsername {
  creator: string;
  username: string;
  controlPubkey: Uint8Array;
  firstCommitment: Uint8Array;
  identitySig: Uint8Array;
  payload: Uint8Array;
}

/** MsgRegisterUsernameResponse defines the response for RegisterUsername. */
export interface MsgRegisterUsernameResponse {
}

/** MsgAddDevice adds a new device commitment to an existing identity. */
export interface MsgAddDevice {
  creator: string;
  username: string;
  newCommitment: Uint8Array;
  identitySig: Uint8Array;
  payload: Uint8Array;
  controlPubkey: Uint8Array;
}

/** MsgAddDeviceResponse defines the response for AddDevice. */
export interface MsgAddDeviceResponse {
}

/** MsgRevokeDevice revokes an existing device commitment. */
export interface MsgRevokeDevice {
  creator: string;
  username: string;
  deviceIndex: number;
  identitySig: Uint8Array;
  payload: Uint8Array;
  controlPubkey: Uint8Array;
}

/** MsgRevokeDeviceResponse defines the response for RevokeDevice. */
export interface MsgRevokeDeviceResponse {
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgRegisterUsername(): MsgRegisterUsername {
  return {
    creator: "",
    username: "",
    controlPubkey: new Uint8Array(0),
    firstCommitment: new Uint8Array(0),
    identitySig: new Uint8Array(0),
    payload: new Uint8Array(0),
  };
}

export const MsgRegisterUsername: MessageFns<MsgRegisterUsername> = {
  encode(message: MsgRegisterUsername, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.controlPubkey.length !== 0) {
      writer.uint32(26).bytes(message.controlPubkey);
    }
    if (message.firstCommitment.length !== 0) {
      writer.uint32(34).bytes(message.firstCommitment);
    }
    if (message.identitySig.length !== 0) {
      writer.uint32(42).bytes(message.identitySig);
    }
    if (message.payload.length !== 0) {
      writer.uint32(50).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterUsername {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterUsername();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.controlPubkey = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.firstCommitment = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.identitySig = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterUsername {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      controlPubkey: isSet(object.controlPubkey)
        ? bytesFromBase64(object.controlPubkey)
        : isSet(object.control_pubkey)
        ? bytesFromBase64(object.control_pubkey)
        : new Uint8Array(0),
      firstCommitment: isSet(object.firstCommitment)
        ? bytesFromBase64(object.firstCommitment)
        : isSet(object.first_commitment)
        ? bytesFromBase64(object.first_commitment)
        : new Uint8Array(0),
      identitySig: isSet(object.identitySig)
        ? bytesFromBase64(object.identitySig)
        : isSet(object.identity_sig)
        ? bytesFromBase64(object.identity_sig)
        : new Uint8Array(0),
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRegisterUsername): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.controlPubkey.length !== 0) {
      obj.controlPubkey = base64FromBytes(message.controlPubkey);
    }
    if (message.firstCommitment.length !== 0) {
      obj.firstCommitment = base64FromBytes(message.firstCommitment);
    }
    if (message.identitySig.length !== 0) {
      obj.identitySig = base64FromBytes(message.identitySig);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRegisterUsername>, I>>(base?: I): MsgRegisterUsername {
    return MsgRegisterUsername.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRegisterUsername>, I>>(object: I): MsgRegisterUsername {
    const message = createBaseMsgRegisterUsername();
    message.creator = object.creator ?? "";
    message.username = object.username ?? "";
    message.controlPubkey = object.controlPubkey ?? new Uint8Array(0);
    message.firstCommitment = object.firstCommitment ?? new Uint8Array(0);
    message.identitySig = object.identitySig ?? new Uint8Array(0);
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRegisterUsernameResponse(): MsgRegisterUsernameResponse {
  return {};
}

export const MsgRegisterUsernameResponse: MessageFns<MsgRegisterUsernameResponse> = {
  encode(_: MsgRegisterUsernameResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterUsernameResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterUsernameResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterUsernameResponse {
    return {};
  },

  toJSON(_: MsgRegisterUsernameResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRegisterUsernameResponse>, I>>(base?: I): MsgRegisterUsernameResponse {
    return MsgRegisterUsernameResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRegisterUsernameResponse>, I>>(_: I): MsgRegisterUsernameResponse {
    const message = createBaseMsgRegisterUsernameResponse();
    return message;
  },
};

function createBaseMsgAddDevice(): MsgAddDevice {
  return {
    creator: "",
    username: "",
    newCommitment: new Uint8Array(0),
    identitySig: new Uint8Array(0),
    payload: new Uint8Array(0),
    controlPubkey: new Uint8Array(0),
  };
}

export const MsgAddDevice: MessageFns<MsgAddDevice> = {
  encode(message: MsgAddDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.newCommitment.length !== 0) {
      writer.uint32(26).bytes(message.newCommitment);
    }
    if (message.identitySig.length !== 0) {
      writer.uint32(34).bytes(message.identitySig);
    }
    if (message.payload.length !== 0) {
      writer.uint32(42).bytes(message.payload);
    }
    if (message.controlPubkey.length !== 0) {
      writer.uint32(50).bytes(message.controlPubkey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAddDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAddDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newCommitment = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.identitySig = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.controlPubkey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAddDevice {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      newCommitment: isSet(object.newCommitment)
        ? bytesFromBase64(object.newCommitment)
        : isSet(object.new_commitment)
        ? bytesFromBase64(object.new_commitment)
        : new Uint8Array(0),
      identitySig: isSet(object.identitySig)
        ? bytesFromBase64(object.identitySig)
        : isSet(object.identity_sig)
        ? bytesFromBase64(object.identity_sig)
        : new Uint8Array(0),
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      controlPubkey: isSet(object.controlPubkey)
        ? bytesFromBase64(object.controlPubkey)
        : isSet(object.control_pubkey)
        ? bytesFromBase64(object.control_pubkey)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgAddDevice): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.newCommitment.length !== 0) {
      obj.newCommitment = base64FromBytes(message.newCommitment);
    }
    if (message.identitySig.length !== 0) {
      obj.identitySig = base64FromBytes(message.identitySig);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.controlPubkey.length !== 0) {
      obj.controlPubkey = base64FromBytes(message.controlPubkey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAddDevice>, I>>(base?: I): MsgAddDevice {
    return MsgAddDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAddDevice>, I>>(object: I): MsgAddDevice {
    const message = createBaseMsgAddDevice();
    message.creator = object.creator ?? "";
    message.username = object.username ?? "";
    message.newCommitment = object.newCommitment ?? new Uint8Array(0);
    message.identitySig = object.identitySig ?? new Uint8Array(0);
    message.payload = object.payload ?? new Uint8Array(0);
    message.controlPubkey = object.controlPubkey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgAddDeviceResponse(): MsgAddDeviceResponse {
  return {};
}

export const MsgAddDeviceResponse: MessageFns<MsgAddDeviceResponse> = {
  encode(_: MsgAddDeviceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAddDeviceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAddDeviceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgAddDeviceResponse {
    return {};
  },

  toJSON(_: MsgAddDeviceResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAddDeviceResponse>, I>>(base?: I): MsgAddDeviceResponse {
    return MsgAddDeviceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAddDeviceResponse>, I>>(_: I): MsgAddDeviceResponse {
    const message = createBaseMsgAddDeviceResponse();
    return message;
  },
};

function createBaseMsgRevokeDevice(): MsgRevokeDevice {
  return {
    creator: "",
    username: "",
    deviceIndex: 0,
    identitySig: new Uint8Array(0),
    payload: new Uint8Array(0),
    controlPubkey: new Uint8Array(0),
  };
}

export const MsgRevokeDevice: MessageFns<MsgRevokeDevice> = {
  encode(message: MsgRevokeDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.deviceIndex !== 0) {
      writer.uint32(24).uint64(message.deviceIndex);
    }
    if (message.identitySig.length !== 0) {
      writer.uint32(34).bytes(message.identitySig);
    }
    if (message.payload.length !== 0) {
      writer.uint32(42).bytes(message.payload);
    }
    if (message.controlPubkey.length !== 0) {
      writer.uint32(50).bytes(message.controlPubkey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.deviceIndex = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.identitySig = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.controlPubkey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRevokeDevice {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      deviceIndex: isSet(object.deviceIndex)
        ? globalThis.Number(object.deviceIndex)
        : isSet(object.device_index)
        ? globalThis.Number(object.device_index)
        : 0,
      identitySig: isSet(object.identitySig)
        ? bytesFromBase64(object.identitySig)
        : isSet(object.identity_sig)
        ? bytesFromBase64(object.identity_sig)
        : new Uint8Array(0),
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      controlPubkey: isSet(object.controlPubkey)
        ? bytesFromBase64(object.controlPubkey)
        : isSet(object.control_pubkey)
        ? bytesFromBase64(object.control_pubkey)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRevokeDevice): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.deviceIndex !== 0) {
      obj.deviceIndex = Math.round(message.deviceIndex);
    }
    if (message.identitySig.length !== 0) {
      obj.identitySig = base64FromBytes(message.identitySig);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.controlPubkey.length !== 0) {
      obj.controlPubkey = base64FromBytes(message.controlPubkey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRevokeDevice>, I>>(base?: I): MsgRevokeDevice {
    return MsgRevokeDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRevokeDevice>, I>>(object: I): MsgRevokeDevice {
    const message = createBaseMsgRevokeDevice();
    message.creator = object.creator ?? "";
    message.username = object.username ?? "";
    message.deviceIndex = object.deviceIndex ?? 0;
    message.identitySig = object.identitySig ?? new Uint8Array(0);
    message.payload = object.payload ?? new Uint8Array(0);
    message.controlPubkey = object.controlPubkey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRevokeDeviceResponse(): MsgRevokeDeviceResponse {
  return {};
}

export const MsgRevokeDeviceResponse: MessageFns<MsgRevokeDeviceResponse> = {
  encode(_: MsgRevokeDeviceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeDeviceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeDeviceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRevokeDeviceResponse {
    return {};
  },

  toJSON(_: MsgRevokeDeviceResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRevokeDeviceResponse>, I>>(base?: I): MsgRevokeDeviceResponse {
    return MsgRevokeDeviceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRevokeDeviceResponse>, I>>(_: I): MsgRevokeDeviceResponse {
    const message = createBaseMsgRevokeDeviceResponse();
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /**
   * UpdateParams defines a (governance) operation for updating the module
   * parameters. The authority defaults to the x/gov module account.
   */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** RegisterUsername registers a new unique username with an initial device commitment. */
  RegisterUsername(request: MsgRegisterUsername): Promise<MsgRegisterUsernameResponse>;
  /** AddDevice adds a new device commitment to an existing identity. */
  AddDevice(request: MsgAddDevice): Promise<MsgAddDeviceResponse>;
  /** RevokeDevice revokes an existing device commitment. */
  RevokeDevice(request: MsgRevokeDevice): Promise<MsgRevokeDeviceResponse>;
}

export const MsgServiceName = "surchain.identity.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.RegisterUsername = this.RegisterUsername.bind(this);
    this.AddDevice = this.AddDevice.bind(this);
    this.RevokeDevice = this.RevokeDevice.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(new BinaryReader(data)));
  }

  RegisterUsername(request: MsgRegisterUsername): Promise<MsgRegisterUsernameResponse> {
    const data = MsgRegisterUsername.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterUsername", data);
    return promise.then((data) => MsgRegisterUsernameResponse.decode(new BinaryReader(data)));
  }

  AddDevice(request: MsgAddDevice): Promise<MsgAddDeviceResponse> {
    const data = MsgAddDevice.encode(request).finish();
    const promise = this.rpc.request(this.service, "AddDevice", data);
    return promise.then((data) => MsgAddDeviceResponse.decode(new BinaryReader(data)));
  }

  RevokeDevice(request: MsgRevokeDevice): Promise<MsgRevokeDeviceResponse> {
    const data = MsgRevokeDevice.encode(request).finish();
    const promise = this.rpc.request(this.service, "RevokeDevice", data);
    return promise.then((data) => MsgRevokeDeviceResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
